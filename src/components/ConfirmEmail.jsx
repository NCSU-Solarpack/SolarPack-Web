// ConfirmEmail component removed: email confirmation flow is not used anymore.
// Keeping a minimal placeholder export to avoid import errors if any remain.

const ConfirmEmail = () => null;

export default ConfirmEmail;
